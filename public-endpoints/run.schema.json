{
  "title": "AgentBase Run Endpoint Schema",
  "description": "Schema for validating requests to the /run public endpoint.",
  "type": "object",
  "properties": {
    "agent": {
      "type": "string",
      "description": "Identifier for the calling agent."
    },
    "task": {
      "type": "string",
      "description": "The task ID to execute."
    },
    "input": {
      "type": "object",
      "description": "Input object matching the task's input schema."
    }
  },
  "required": ["agent", "task", "input"],
  "additionalProperties": false
}
